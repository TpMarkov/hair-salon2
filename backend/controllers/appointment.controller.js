// API for create new appointment
const createAppointment = async (req, res) => {

  try {

    const {serviceData, slotDate, slotTime, canceled = false, isComplete = false, date = new Date()} = req.body

  } catch (err) {

  }
}